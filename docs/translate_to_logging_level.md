## translate_to_logging_level (FunctionDef)

Translates a string to a ps_logging level for Python.

Arguments:
    level_str (str, required):
        The string that you'd like converted to a log-level.

---
